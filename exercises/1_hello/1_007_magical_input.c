#include <stdio.h>

/**
 * Exercise: Magical Input
 * 
 * A wizard needs to collect information about a spell from the user.
 * Help them create a program that asks for a spell number.
 * 
 * Your task:
 * 1. declare correct type for the variable to store the spell number
 * 2. Use scanf to read the number from the user
 * 
 * Expected output:
 * Enter spell number: 42
 * You selected spell #42!
 */

int main() {
    // Declare with the correct type
    spellNumber;
    
    // Ask the user to enter a spell number
    printf("Enter spell number: ");
    
    // Use scanf to read the input
    
    printf("You selected spell #%d!\n", spellNumber);
    
    return 0;
}
